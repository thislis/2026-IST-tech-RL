# v7 구현 및 실험 계획서
## v7-1: FlyWire 부분 회로 기반 행동 보정 / v7-2: 전뇌 시뮬레이션 기반 게임 제어

작성일: 2026-09-14 (Asia/Seoul)  
대상 프로젝트: `thislis/2026-IST-tech-RL`  
기준 커밋: `2842fb17aeb6923f55c391d04de182df758f02e3` — 2026-09-10 병합된 v6  
문서 상태: **설계 제안. 저장소·공개 자료 검토를 바탕으로 작성했으며, v7 코드 구현·커넥톰 원본 다운로드·Unity 학습·승률 측정은 실행하지 않았다.**

본문의 `[Rxx]`는 프로젝트 코드·문서, `[Sxx]`는 외부 1차 자료를 가리킨다. 전체 출처와 고정된 소스 주소는 마지막 절에 있다. 실험 예산, 모델 크기, 하이퍼파라미터, 승격 기준은 별도 표시가 없는 한 **이 문서의 제안값**이며 측정 결과가 아니다. 새 파일명과 실행 명령은 앞으로 구현할 인터페이스다.

---

## 0. 의사결정 요약

두 옵션은 같은 모델의 크기만 달리하는 실험이 아니다. **v7-1은 기존 게임 AI에 생물학적 배선을 구조적 제약으로 추가하는 경로이고, v7-2는 감각 입력을 받은 전뇌 모형의 활동이 게임 행동을 발생시키도록 만드는 경로**다.

| 구분 | v7-1 — 부분 회로 행동 보정 | v7-2 — 전뇌 기반 직접 제어 |
|---|---|---|
| 우선 목적 | 기존 v6의 성능·학습 효율 개선 | 둠·마리오 사례와 같은 폐루프 제어 재현 및 학습 가능성 검증 |
| 기본 데이터 | FlyWire FAFB v783 | MaleCNS v1.0; FlyWire 데이터로 바꾸는 실험은 별도 등록 |
| 뇌 구조 활용 | 작은 실제 부분 그래프의 연결 마스크 | 명시한 포함 기준을 만족하는 전체 신경 그래프 |
| 기존 계획기 | 유지 | 최종 직접 제어 경로에서는 제거 |
| 기본 출력 | 기존 KEEP + 5유닛×8보정 = 41-way | 유닛별 직접 이동·정지; 학습형은 9-way |
| 학습 | 기존 특징 뒤의 그래프 모듈과 출력층을 PPO로 학습 | 무학습 고정 제어 → 고정 전뇌의 출력층 학습 → 선택적 내부 가소성 |
| 시간적 신경 상태 | 새 그래프 모듈은 호출 간 상태를 유지하지 않음 | 막전위·발화 이력·감각 필터 등을 유지 |
| 팀 확장 | 공유 보정 모듈을 기존 5개 슬롯에 적용 | 1유닛 검증 후 동일 배선·독립 상태의 5개 인스턴스 |
| 1차 성공 | 동일 조건의 MLP 대비 유용한 개선 | 입력→신경 활동→행동의 인과적 연결 확인 |
| 추가 성공 | 배선 재연결 대조군보다 우수 | 학습 전·무학습 대조군 대비 미관측 과제 성능 개선 |
| 대회 제출 | 기존 계약을 보존하는 방향이지만 계획기의 상태 유지 문제는 남음 | 상태 유지·자원·의존성 허용 확인 전에는 연구용 |

**권고:** v7-1을 성능 개선의 주 경로로, v7-2를 독립적인 연구 경로로 개발한다. v7-2가 불리하다는 이유로 처음부터 부분 그래프나 계획기 보정 방식으로 바꾸면 사용자가 요청한 두 번째 옵션을 검증한 것이 아니다. 반대로 전뇌가 움직임을 만들었다는 이유로 대회 성능이 향상됐다고 발표하지 않는다.

---

## 1. 웹 조사 결과: ‘초파리가 게임을 한다’는 사례의 정확한 의미

### 1.1 Fly64 — Super Mario 64

확인한 공개 구현은 `ornata/fly`의 **Fly64**다. MaleCNS 배선에 감각 입력과 단순화된 신경 동역학을 결합하고, 선택한 하행 뉴런 집단의 활동을 게임 조작으로 변환한다. 저자는 학습·보상·별 수집 목표가 없다고 명시한다. 따라서 이것은 **신경 연결망 기반 모형이 실제 게임을 제어하는 데모**이지, 게임 공략을 학습했다는 결과가 아니다. [S05, S06]

설계상 참고할 점은 감각·신경·조작을 분리한 구조와 재생 검증이다. 실제 구현은 별도 시야를 감각 세포에 투영하고, 신경 계산과 조작을 시간에 맞춰 진행한다. 다만 광학 대응과 조작 해석은 공학적 근사다. 신경 출력이 조작 요청을 만들었다는 사실과 게임 엔진이 그 행동을 실행했다는 사실도 따로 기록한다. [S05, S06]

### 1.2 DOOMFLY — Doom 엔진

확인한 공개 구현은 `nftechie/doomfly`다. 현재 README 및 2026-09-05 학습 프로토콜은 MaleCNS 전체 유지 그래프에 시각 자극을 넣고, 고정 인터페이스로 이동·회전·발사를 제어한다고 설명한다. 일부 기존 연결에 가소성 규칙을 적용한 실험도 있으나, **시각 반응·조건화·생존 검증을 통과하지 못했으며 학습 성공을 입증하지 못했다**고 명시한다. [S07, S08]

같은 저장소의 검토 문서에는 과거 고정 모델에 대한 ‘학습 없음’ 설명도 남아 있다. 최신 학습 프로토콜과 과거 기준 모델을 섞지 않는다. 특히 어두운 입력에서도 배경 구동 때문에 행동이 나오는 현상이 검토되어 있으므로, 움직임이나 간헐적 성공만으로 화면 이해를 주장하지 않는다. [S08, S09]

### 1.3 FlyWire와 MaleCNS는 동일 데이터가 아니다

FlyWire FAFB는 성체 암컷 뇌 데이터다. 위 두 게임 구현의 기본 데이터는 **수컷의 뇌와 복측 신경삭을 포함하는 MaleCNS v1.0**이다. Codex가 여러 데이터셋을 탐색할 수 있다는 사실이 개체·뉴런 ID·버전의 동일성을 의미하지 않는다. [S01, S02, S04]

본 계획은 사용자가 언급한 게임 방식을 충실히 반영하기 위해 v7-2의 기본 데이터로 MaleCNS를 선택한다. 따라서 v7 전체를 ‘FlyWire만 사용한 프로젝트’라고 부르지 않는다. FlyWire로 통일해야 할 경우 `v7_2_fafb783`을 별도 실험으로 만들고, 감각 세포·출력 세포 대응을 다시 검증한다. MaleCNS의 세포 ID를 FAFB ID로 복사하거나, 데이터셋만 바꾼 채 기존 망막 대응을 재사용하지 않는다.

### 1.4 논문이 뒷받침하는 범위

Shiu 등의 2024년 연구는 커넥톰 기반 leaky integrate-and-fire 모델로 섭식·그루밍 회로의 반응을 예측하고 실험과 비교했다. 이는 계산 모형의 근거지만 게임 제어·게임 학습의 검증은 아니다. [S10]

중심복합체 연결망 연구는 방향·항법·행동 선택에 관련된 부분 회로를 고를 근거를 제공한다. 해당 연구의 주요 데이터와 FlyWire를 혼동하지 않고 세포 유형의 대응을 확인해야 한다. [S11, S15]

Lappalainen 등의 연구는 연결 정보에 동역학과 학습을 결합해 시각계 활동을 예측했다. 시각 세포를 모두 동일한 스파이킹 뉴런으로 취급하는 단순 모델이 자동으로 정당화되는 것은 아니다. [S12]

FlyGM은 연결망을 학습 가능한 제어 구조로 활용한 선례다. 이 문서에서는 이전 답변에서 언급한 최초 공개본이 아니라 **2026-06-14 수정된 arXiv v3**를 기준으로 한다. 전문가 모방 후 PPO 학습을 사용한 신체 운동 제어 연구이며, 무학습 게임 데모와 구분한다. BlackOut에서의 효과는 별도 검증 대상이다. [S13]

---

## 2. 현재 v6에서 출발할 때 지켜야 하는 계약

### 2.1 확인된 코드 구조

기준 커밋의 v6는 기존 인코더를 고정하고, 인코더 특징과 49차원 계획기 문맥을 `V6Model.residual`에 넣는다. 유닛별 8개 보정 점수를 합쳐 KEEP을 포함한 41개 팀 선택지를 만든다. 한 시점에 수정하는 유닛은 최대 하나다. 역할 기본값은 worker 3명, guard 2명이다. [R02]

```text
관측 ── 고정 인코더 ── latent ──────────────┐
관측 ── 상태 유지 계획기 ── 49차원 문맥 ───┤
                                          ↓
                            유닛별 보정 점수 5×8
                                          ↓
                      KEEP을 포함하는 팀 단위 41-way
                                          ↓
                         계획기 행동 또는 한 유닛 보정
```

PPO 수집기는 특징을 `detach()`한 뒤 저장하고, 업데이트 때 그 특징을 사용한다. **새 학습 모듈을 특징 생성 앞쪽에 넣고 수집기까지 그대로 두면 새 모듈로 학습 신호가 돌아가지 않는다.** v7-1은 저장된 특징 뒤에 그래프 모듈을 둔다. [R03]

현재 README와 v6 변경 보고서는 실제 v6 학습·승률 달성을 보고하지 않는다. 계획서의 목표 승률을 기준 모델의 측정 성능으로 사용하지 않는다. [R01, R04]

### 2.2 게임 입출력과 좌표

관측 계약은 96차원 벡터와 11채널 의미 지도다. 제출 입력은 벡터 `(B,96)`, 지도 `(B,11,96,96)`이며 행동은 유닛마다 `(dx,dy)`다. 비영벡터는 게임에서 정규화되므로 크기를 줄여도 느리게 걷는 명령이 되지 않는다. `(0,0)`만 정확한 정지다. [R05, R06, R07]

v7-2에서도 신경 발화율을 벡터 크기에 직접 대응시켜 속도 제어라고 해석하지 않는다. 발화율은 방향·정지 임계값·별도 명시한 이동 빈도로 변환할 수 있다. 기본 실험은 방향과 정지만 사용한다.

게임의 A/B 진영 대칭은 `y=x` 반사이고, 맵 영상의 행 방향과 세계 Y 방향도 다르다. 기존 변환을 재사용하고, 게임 좌표·가상 시야 좌표·초파리 좌우 세포 주석을 각각 독립적으로 검사한다. [R02, R04]

### 2.3 평가·제출 계약의 미확인 항목

저장소에는 배치 순서, 상태 유지, 경기 초기화 신호, 추론 제한, 추가 의존성 등 공식 확인이 필요한 항목이 남아 있다. v7-1의 새 신경망이 비상태형이어도 기존 계획기는 상태를 가진다. v7-2는 더욱 명시적인 상태 초기화 계약이 필요하다. [R05]

최종 승패는 프로젝트 내부 평가처럼 terminal `winner`를 기준으로 측정한다. 대회 공식 판정이 다른 경우 이를 별도 보고하되 내부 승패 정의를 몰래 바꾸지 않는다. v6의 dev·confirmation은 개발 회귀 확인에 쓰고, 이미 선택에 사용된 결과를 최종 독립 테스트로 부르지 않는다. [R04, R05]

---

## 3. 두 경로가 공유할 데이터·재현성 계층

### 3.1 데이터 선택과 취득

**v7-1:** `dataset_key=flywire_fafb`, `snapshot=783`. Codex 정적 다운로드에서 연결 정보와 세포 유형·좌우·영역 주석을 확보한다. 실제 데이터 상품 이름, 검출기, 생성일, 임계값과 파일 해시를 기록한다. 버전 783이라도 통합 주석 파일이 갱신될 수 있으므로 버전 번호만으로 재현성을 보장하지 않는다. Codex 인증 토큰은 환경변수로만 전달하고, URL·로그·문서·체크포인트에 넣지 않는다. [S02, S03]

**v7-2:** `dataset_key=male_cns`, `release=v1.0`. 정본은 Janelia 공식 다운로드의 neuron annotations, neurotransmitters, weighted connections 파일로 정한다. 전자현미경 원영상이나 모든 시냅스 좌표는 최초 구현에 필요하지 않다. 기본 입력은 다음 세 파일이다. [S04]

```text
body-annotations-male-cns-v1.0-minconf-0.5.feather
body-neurotransmitters-male-cns-v1.0.feather
connectome-weights-male-cns-v1.0-minconf-0.5.feather
```

공식 연결 파일의 압축·저장 크기와 실행 중 메모리는 다르다. 다운로드 크기를 RAM 요구량으로 사용하지 않는다.

Codex의 기본 표시 임계값은 FAFB에서 연결쌍당 5개 시냅스다. 동일 연결쌍이 영역별 여러 행으로 존재할 수 있다. **웹 탐색기에 보이는 연결 수와 원본 전체 유지 그래프의 연결 수를 그대로 비교하지 않는다.** v7-1은 실제 제공 파일의 임계값을 계승하고, 더 낮은 임계값의 원자료를 확보한 경우에만 임계값 민감도 실험을 추가한다. 누락된 약한 연결을 임의 생성하지 않는다. [S02]

### 3.2 통일된 그래프 표현

신규 `GraphArtifact`의 최소 필드는 다음과 같다. 원본 ID는 문자열 또는 손실 없는 정수로 읽고, 배열 연산용 연속 인덱스와 분리한다.

```text
schema_version
source_dataset / source_version / annotation_version / detector_version
source_files: [{name, source_url_without_token, sha256, license}]
node_ids: original ID ↔ contiguous index
node_attributes: cell_type, side, region, transmitter, confidence
edge_src / edge_dst: integer index arrays
synapse_count: original structural counts
model_weight: derived computational weights, if applicable
input_ports / output_ports: source IDs, pooling, rationale, confidence
inclusion_policy / aggregation_policy / threshold_policy
preprocess_config_sha256 / graph_sha256 / node_table_sha256
counts: source_rows, retained_nodes, retained_edges, retained_contacts
exclusions: missing_annotations, excluded_endpoints, other_reasons
```

방향은 항상 `source=presynaptic`, `destination=postsynaptic`으로 정하고, 수식에서는 `W[dst,src]`를 사용한다. 구현과 수식의 방향을 테스트로 대조한다.

집계 시에는 동일한 전후 뉴런에 대한 행이 서로 겹치지 않는 영역별 집계라고 확인한 경우에만 시냅스 수를 더한다. 전체 합계와 영역별 행이 섞인 자료를 이중으로 합산하지 않는다. 계산 모델이 사용하지 않는 영역 정보도 원본과의 대응 관계로 보존한다.

원본 취득, 검증, 가공, 학습을 별도 단계로 만든다. 추론에 네트워크 접근을 요구하지 않는다. 토큰이나 다운로드 권한이 없을 때 합성 그래프로 자동 대체하는 처리는 금지한다. 합성 그래프는 테스트 전용의 명시적 옵션으로만 사용한다.

### 3.3 라이선스와 소스 관리

FlyWire 공개 데이터의 공식 가이드는 CC BY-NC 4.0, MaleCNS 공식 페이지는 CC-BY를 명시한다. 데이터 배포 조건과 참조 구현의 코드 라이선스를 별도로 기록한다. 기업 활용이나 모델 내 포함·재배포에 관한 판단이 필요하면 공개 전에 권리자 확인 항목으로 처리한다. 모든 산출물을 자동으로 MIT 라이선스로 취급하지 않는다. [S03, S04]

DOOMFLY는 자체 코드에 MIT를 명시하지만 의존물·데이터는 별도 조건이다. Fly64도 재사용할 구체적인 파일의 라이선스를 확인하기 전에는 코드를 이식하지 않는다. 모델의 아이디어를 참고하는 것과 코드 복제 권한을 확보하는 것은 구분한다. [S07]

대용량 원본을 Git에 넣기보다 취득·가공 절차, 해시, 필요한 출처 표시를 보관한다. 배포할 `checkpoint.pt`에 그래프를 포함할 때도 이 확인을 거친다.

---

## 4. v7-1 상세 설계 — 작은 부분 회로로 행동 보정

### 4.1 연구 질문과 변경 범위

연구 질문은 “**기존 게임 특징과 계획기를 동일하게 유지할 때, FlyWire 배선이 일반적인 보정 MLP보다 유용한 학습을 유도하는가?**”다.

변경 대상은 `V6Model.residual`에 해당하는 부분으로 좁힌다. 고정 인코더, 49차원 문맥, critic 입력·구조, 41-way 분포, 행동 마스크, 보정 방향 생성, 게임 보상은 1차 비교에서 유지한다. 5개 슬롯은 동일한 그래프 구조와 학습 가중치를 공유하지만 입력 특징과 계산 상태는 독립적이다.

새 모듈이 과거 시점을 기억한다고 주장하지 않는다. 현재 특징에 포함된 계획기의 이력 요약은 사용하지만, 그래프 내부 상태는 호출마다 초기화한다.

### 4.2 부분 회로 선택

첫 후보는 중심복합체의 방향·항법 관련 회로와 입출력에 필요한 주변 연결이다. EPG, PEN, PEG, Delta7, PFL 계열 등의 세포 유형은 탐색 후보이며, **이 문서에서 FAFB v783의 구체적인 뉴런 ID나 최종 포함 개수를 확정한 것은 아니다.** 실제 다운로드 주석에서 대응 유형명, 좌우, 영역을 확인한다. [S11, S15]

먼저 문헌에 근거한 시작 세포 유형 집합을 정하고, 관측을 넣을 입력 지점과 활동을 읽을 출력 지점을 정한다. 그 사이의 방향성 경로와 필요한 되먹임 연결을 포함한다. 이후 잘려나간 외부 입출력 비율, 고립 노드, 강연결 요소, 좌우 불균형을 기록한다. 잘 되는 게임 시드만 보고 회로를 바꾸지 않는다.

초기 구현의 크기 목표는 **실제 뉴런 256~2,048개, 유지 간선 10만 개 이하**다. 해부학적으로 의미 있는 회로를 이 범위 안에 구성할 수 있는지는 아직 확인하지 않았다. 초과하면 더 좁은 회로를 사전에 선택하거나 별도 예산으로 확장한다. 무작위로 잘라내고 같은 회로라고 부르지 않는다. 세포 유형별로 집계하면 `type_aggregated`로 표시하고 뉴런 단위 모델과 분리한다.

### 4.3 계산 모델

```text
x: [..., 5, encoder_hidden + 49]
      ↓ 공유 입력 변환기
u: [..., 5, input_port_count, node_channels]
      ↓ 고정된 회로 입력 지점으로 배치
작은 방향성 그래프: 한 정책 호출 안에서 K회 정보 전달
      ↓ 고정 출력 집계 + 공유 출력층
correction_logits: [..., 5, 8]
      ↓ KEEP logit = 0 추가
joint_logits: [..., 41]
```

초기 제안값은 `node_channels=4`, `input_port_count=16`, `output_pool_count=16`, `K=4`, `leak=0.5`다. 실제 입력·출력 집합이 더 작으면 그 집합에 맞춘다. 모든 뉴런에 직접 연결되는 거대한 학습 입력층이나, 원래 특징에서 행동으로 곧장 가는 우회 연결은 추가하지 않는다.

다음은 이 계획에서 제안하는 계산식이며 실제 막전위를 재현하는 식이 아니다.

```text
H(0) = 0
U = input_adapter(x)
Wθ[dst, src] = M[dst, src] * θ[dst, src]
Wbarθ[dst, :] = Wθ[dst, :] / max(1, sum(abs(Wθ[dst, :])))
H(k+1) = (1-α) H(k) + α tanh(Wbarθ H(k) + P_in U + b)
y = output_head(flatten(Q_out H(K)))
```

`M`은 고정 연결 마스크, `θ`는 존재하는 간선에만 두는 학습 파라미터, `P_in`과 `Q_out`은 기록된 입력 배치와 출력 집계다. 없는 간선에는 파라미터를 두지 않고, 간선 인덱스와 가중치를 별도 배열로 보관한다.

주 실험은 **연결 존재 여부만 사용하는 topology-only**로 정하고 신경전달물질에 따른 부호를 강제하지 않는다. 초기 가중치 분포를 배선 재연결 대조군과 맞춰 구체적인 배선의 효과를 조사한다. 시냅스 수 기반 초기화, 고정 시냅스 가중치, 부호 제약은 후속 실험으로 분리한다. 시냅스 수나 전달물질 예측이 실제 신호 전달 효율을 유일하게 결정한다고 가정하지 않는다. [S10, S12]

`K=4`로 입력에서 출력까지 정보가 도달하는지 빌드 시 검증한다. 위 수식은 첫 업데이트에서 입력을 주입하므로, 처음부터 출력에 영향을 주는 경로는 최대 K-1개 간선을 통과한다. 이 계산 순서를 기준으로 도달성을 검사한다. 도달하지 않는다고 원래 입력을 출력층에 우회시켜 넣지 않는다. 경로 길이에 맞춰 K나 부분 회로를 수정하고, 변경된 조건을 그래프 대조군에도 적용한다.

### 4.4 초기 동작과 PPO 연결

출력 최종층을 영 가중치로 초기화하고 bias로 v6와 같은 초기 보정 확률 설정을 재현한다. KEEP logit은 0을 유지한다. 마스크 적용 전 설정값과 실제 유효 행동에서의 보정률은 구분해 기록한다. 탐색 혼합분포와 PPO 재평가는 기존 `joint_distribution`을 공유한다. [R02]

학습 경로는 반드시 다음과 같아야 한다.

```text
rollout에 저장된 frozen latent + planner context
    → GraphResidualHead: 업데이트 때 다시 계산
    → 동일 마스크·동일 탐색 혼합률의 41-way 분포
    → PPO loss
    → 입력 변환기 / 간선 가중치 / 출력층 업데이트
```

최종층 가중치가 처음에 0이면 첫 역전파에서 앞쪽 모듈의 기울기가 0일 수 있다. 기울기 테스트는 영이 아닌 시험용 최종층을 사용한 경우와 초기화 후 여러 번 업데이트한 경우를 구분한다. 첫 단계의 앞쪽 기울기가 0이라는 사실만으로 오류로 판단하지 않는다.

기존 buffer를 사용할 수 있는 이유는 새 모듈이 고정 인코더 뒤에 있기 때문이다. 인코더까지 학습하는 확장은 원본 관측 저장 또는 재계산을 추가하고 별도 실험으로 관리한다. [R03]

### 4.5 v7-1 테스트와 실패 분석

단위 테스트에는 간선 방향, 희소·밀집 계산 일치, 미연결 간선의 불변성, 출력 형태, 입력 의존성, 기울기 전달, 동일 입력 결정성, A/B 좌표, KEEP 초기화, 마스크, 업데이트 전 PPO 비율 1, 저장·복원 일치를 포함한다.

통합 테스트에서는 v7 래퍼에 기존 MLP를 먼저 넣어 같은 입력열에서 v6와 행동·log-prob·업데이트 결과가 일치하는지 확인한다. 래퍼만 바꿨는데 동작이 달라지면 그래프 실험을 시작하지 않는다.

보정 빈도만 늘고 승률이 바뀌지 않으면 성과로 인정하지 않는다. 보정 이후 국면, 올바른 계획기 행동을 망가뜨린 빈도, 정지·왕복·벽 근처 정체, 진영 차이를 분석한다. 최대 한 유닛만 수정할 수 있는 제약이 핵심 병목이면 동시 다중 유닛 수정은 별도 행동 공간 실험으로 분리한다.

---

## 5. v7-2 상세 설계 — 전뇌 모형이 직접 게임을 제어

### 5.1 정체성을 유지하는 최소 조건

v7-2의 핵심 경로는 다음과 같다.

```text
BlackOut의 허용 관측
    ↓ 감각 변환기
감각 뉴런에 대한 자극
    ↓ 전체 유지 커넥톰의 시간적 동역학
출력 뉴런 집단의 활동
    ↓ 명시적인 신경 활동→조작 변환기
게임 이동·정지
    ↓ 다음 관측
다음 신경 계산
```

계획기의 목표·경로·추천 행동이나 별도 이미지→행동 네트워크가 조작을 대신하면 안 된다. 목표를 알려주는 화살표를 감각 영상에 그리는 것, 가까운 배터리 방향을 출력층에 직접 전달하는 것, 신경 활동이 약할 때 A*로 대신 움직이는 것도 주 실험에서는 금지한다.

검증 초기에 나머지 아군 4개를 기존 정책으로 움직이는 실험은 허용하되 **‘한 유닛 전뇌 제어 + 네 유닛 기존 정책’이라는 혼합팀 실험**으로 표시한다. 이를 전뇌 기반 5유닛 협동 성공으로 발표하지 않는다.

### 5.2 전체 그래프의 범위

게임 참조 구현은 주석을 가진 뉴런을 유지하는 정책에 따라 **166,700개 노드, 25,582,938개 방향성 연결, 124,177,617개 시냅스 접촉**을 보고한다. 이것들은 참조 구현의 포함 규칙 아래 나온 값이며, 이번 작업에서 원본을 다시 집계한 결과는 아니다. 연결쌍 수와 개별 시냅스 접촉 수를 혼동하지 않는다. [S06, S08, S09]

재현 모드는 동일한 공식 파일과 포함 규칙을 적용했을 때 이 수치를 재현하는지 검사한다. 다르면 수치를 맞추려고 임의로 노드를 삭제하지 말고 파일 버전·주석 기준·집계 규칙을 조사한다.

전체 유지 그래프 모드에서는 속도를 이유로 약한 간선이나 드물게 발화하는 뉴런을 제거하지 않는다. 발화가 없는 뉴런의 0 기여를 계산에서 생략하는 정확한 희소 연산은 가능하다. 그래프를 줄이는 변형은 `pruned` 또는 `subgraph`로 이름을 달리하고 기본 v7-2 결과에 섞지 않는다.

### 5.3 입력: BlackOut에는 Fly64의 1인칭 카메라가 그대로 존재하지 않는다

현재 확인한 정책 API는 의미 지도와 벡터 관측을 제공한다. 별도 Unity 1인칭 RGB 카메라 API가 존재한다고 가정하지 않는다. 기본 감각 어댑터는 **허용된 관측을 정해진 방식으로 영상화한 합성 감각 입력**이다. 이는 특권 정보를 추가하는 것은 아니지만, 원시 게임 화면을 그대로 보는 것과도 다르다. [R05, R07]

입력 모드는 다음과 같이 분리한다.

| 모드 | 구현과 정보 | 허용되는 표현 |
|---|---|---|
| `S0_semantic_retina` — 기본 | 의미 지도에서 자신 중심의 방향별 시야를 결정적으로 렌더링. 위치는 시점 생성에만 사용 | ‘의미 관측을 영상화해 전뇌 모형에 입력’ |
| `S1_augmented_sensory` — 후속 | S0에 소지품·자신의 클래스·점수·시간 등 합법적 관측을 별도 감각 자극으로 추가 | ‘게임 상태를 포함한 인공 감각 인터페이스’ |
| `S2_native_rgb` — 조건부 | 엔진에서 공식적으로 제공·허용되는 RGB 시야를 확보한 경우 | 그때만 ‘실제 게임 영상 기반 제어’ |

S0의 시작 설계는 수평 270도, `128×64` 합성 시야다. 벽·창고·유닛·아이템을 고정 색·밝기로 표현하고, 벽 가림과 거리 제한을 동일하게 적용한다. 이는 Fly64에서 영감을 받은 **BlackOut용 가상 센서**이며 초파리 광학의 측정값이 아니다. 렌더링 선택 자체가 성능에 영향을 줄 수 있으므로 일반 신경망 대조군에도 동일한 영상을 준다.

S0에는 추천 경로, 목표 순위, 적의 미래 위치, 보상, critic 특징을 넣지 않는다. 자신의 위치·가상 방향을 영상 생성에 사용하더라도 그 숫자를 출력층에 직접 전달하지 않는다. 색상 팔레트·시야 범위·가림 규칙은 학습 전 고정하고, 맵이나 상대에 맞춰 변경하지 않는다.

감각 세포의 망막 위치는 공식 주석과 참조 코드의 대응 자료를 검토해 추출한다. 직접 주석, 연결 기반 추정, 기하학적 대체를 구분한 신뢰도 필드를 저장한다. 좌우 비대칭이나 미대응 세포를 조용히 채우지 않는다. 광학 대응이 미검증이라는 한계를 결과에 남긴다. [S06, S09]

### 5.4 동역학: 재현용 단순 모델과 생리학적 검증을 분리

최초 구현은 `dynamics_id=fly64_proxy_v1`로 이름 붙인 단순 이산시간 전뇌 모델을 제안한다. 신경 tick은 게임 기준 20ms와 맞추고, 감각 영상은 초기에는 100ms마다 갱신하되 그 사이 입력을 유지한다. 이는 Fly64의 느슨한 동역학·감각 갱신 방식을 참고한 설계다. 정확한 식·배경 구동·잡음·정규화는 코드 검토와 단위 검증을 거쳐 manifest에 고정한다. [S06]

필수 상태에는 막전위 대용 변수, 직전 발화, 감각 필터, 출력 집단의 발화 이력, 가상 방향, RNG 상태가 있다. 배경 입력과 잡음은 감각 입력과 별도로 기록한다. 검은 화면에서도 신경 활동이 나온다고 오류는 아니지만, 그 활동이 시각 처리 성과라는 뜻도 아니다.

Shiu/Brian2형 연속시간 LIF를 재현하는 `dynamics_id=shiu_lif_reference`는 별도 검증 모델로 둔다. 0.1ms 적분 모델을 사용한다면 20ms 게임 간격마다 200회 적분해야 한다. **20ms짜리 한 번의 단순 업데이트와 동일한 모델이라고 부르지 않는다.** 작은 회로에서 독립 참조 구현과 불응기·시냅스 지연·발화·리셋을 비교한 뒤 전뇌로 확장한다. [S09, S10]

전달물질 부호는 생리학적 확정값이 아니라 선택한 모델의 가정으로 저장한다. 특히 glutamate·histamine·신경조절 세포의 단순 부호 처리는 별도 민감도 시험 대상이다. 도파민 뉴런을 양의 연결로 넣었다고 보상 학습이 구현된 것은 아니다. [S10, S12]

### 5.5 출력: 뉴런 집단에서 이동·정지로

고정 제어 모드에서는 검토된 하행 뉴런 집단의 좌우 활동 차이를 방향 변화로, 이동 관련 집단 활동을 이동/정지 임계값으로 바꾸는 작은 변환기를 만든다. Fly64의 DNa02/DNg13 등은 조사 출발점이며, BlackOut의 특정 조작에 자연적으로 대응하는 세포라는 주장은 하지 않는다. 선택한 세포 ID·집계 창·이득·임계값·좌우 부호를 기록한다. [S05, S06]

개념적인 인터페이스는 다음과 같다. 각 상수는 검증해야 할 공학적 제안값이다.

```text
turn = clip(k_turn * (rate_right - rate_left), -turn_limit, turn_limit)
virtual_heading_next = virtual_heading + turn * simulated_dt
move = movement_pool_rate > movement_threshold

if move:
    action = (cos(virtual_heading_next), sin(virtual_heading_next))
else:
    action = (0, 0)
```

이 가상 방향은 BlackOut 엔진의 실제 몸 방향을 복원한 값이 아니라 감각·제어 어댑터의 상태다. 방향 갱신 시점과 정지 중 회전 허용 여부를 고정한다. 비교할 때는 같은 규칙을 대조군에도 적용한다. 출력 벡터의 크기로 보행 속도를 조절하지 않는다. [R06]

학습형 출력층은 `NoOp + 8방향`의 9-way categorical을 사용한다. 동일한 행동 공간에서 비교하려면 고정 신경 제어 결과도 같은 9방향으로 변환한 대조군을 둔다. 연속 방향 고정 제어와 9방향 학습 제어의 차이를 학습 효과에 포함시키지 않는다.

### 5.6 v7-2 학습을 세 단계로 분리

**F0 — 고정 전뇌, 고정 조작 변환기, 학습 없음.**

둠·마리오 방식의 최소 재현이다. 뇌와 출력 변환기는 학습하지 않는다. 목적은 신경 모델이 행동에 연결되는지 확인하는 것이다. 게임 승률이 낮아도 제어 실험으로 의미가 있을 수 있지만 학습 성공은 아니다.

**F1 — 고정 전뇌, 출력층만 PPO로 학습. 기본 성능 실험으로 권장한다.**

```text
고정 감각 변환 → 고정 전뇌의 시간적 계산
                  ↓ 선택된 출력 집단의 활동 특징 z_t
                  작은 학습 출력층 → 9-way 행동
```

변하는 파라미터는 출력층과 학습 전용 critic뿐이다. 전뇌 배선·동역학·입력 변환기·신경 특징 정규화는 고정한다. 따라서 이 결과는 **‘전뇌 활동을 특징으로 사용하는 게임 정책의 학습’**이지 ‘초파리의 시냅스가 게임을 배웠다’가 아니다.

고정 전뇌가 만든 `z_t`를 저장한 뒤 출력층을 PPO로 업데이트할 수 있다. 고정된 전체 이력→특징 변환을 사용하므로, 출력층의 현재·과거 행동확률 비교에 전체 전뇌 역전파가 필요하지 않다. 다만 수집 시의 신경 상태와 RNG는 정확히 유지되어야 하고, 입력 변환기나 신경 동역학을 학습하기 시작하면 이 단순한 처리의 전제가 깨진다.

5유닛 학습은 5개 9-way 분포를 조건부로 독립적으로 출력하고, 팀 정책의 log-prob를 합산하는 방식으로 명시한다.

```text
log π_team(a_t | history_t) = Σ(slot=0..4) log π_slot(a_t_slot | z_t_slot)
```

이는 45개의 logit으로 표현하지만 가능한 팀 행동은 `9^5 = 59,049`개다. v6의 41개 제한된 보정 선택과 같은 행동 공간이 아니므로, v7-1과의 승률 차이를 커넥톰 크기의 인과 효과로 해석하지 않는다.

**F2 — 일부 내부 연결에 가소성 추가. 독립적인 후속 연구다.**

전뇌 전체는 그대로 계산하고, 사전에 지정한 실제 KC→MBON 등 일부 존재하는 연결의 효과만 변화시키는 모델을 조사한다. 가소성 위치·도파민 대응·활동 흔적·지연·경계값을 문헌과 세포 대응으로 정한 후 시험한다. 문헌의 기억 모델을 게임에 적용하는 순간 추가 공학 가정이 들어간다. [S08, S14]

F2에서는 출력층을 고정한다. 출력층 PPO와 내부 가소성을 동시에 켜고 어느 부분이 개선을 만들었는지 불명확하게 두지 않는다. 초기 출력층을 F1에서 가져오면 모든 대조군에 동일한 체크포인트를 적용하고 그 선행 학습 비용도 보고한다.

BlackOut에 Doom처럼 사용 가능한 체력·피해 신호가 있다고 가정하지 않는다. 기본 피드백은 확인된 팀 점수 변화와 terminal 결과로 제한한다. 추가 이벤트를 사용하려면 실제 API·관측에서 검출 가능성을 검증한다. 보상은 행동이 발생한 다음에 전달하고, 보상 이벤트 자체가 곧바로 행동을 선택하는 우회 경로를 만들지 않는다.

20초 점수 흡수 지연과 긴 경기 때문에 단순히 즉시 보상을 넣는 것으로 학습이 해결되지 않을 수 있다. 활동의 흔적을 남기는 eligibility trace와 피드백 지연을 별도 설계 변수로 둔다. trace 길이와 가소성 수치는 미니과제에서 검증하기 전 전뇌 경기 실험에 고정값으로 확정하지 않는다.

### 5.7 상태·초기화·시간 계약

기본 평가에서는 **매 경기마다 신경 상태·출력 필터·가상 방향·RNG를 명시적으로 초기화하고, 학습된 파라미터만 유지**한다. 가소성 모델도 평가 시 업데이트를 끄고, 선택된 학습 완료 가중치로 각 경기를 시작한다. 한 평가 경기의 보상과 기억이 다음 경기로 넘어가지 않게 한다.

훈련 중의 경기 간 가소성 가중치 보존과 막전위 초기화를 분리한다. `reset_episode_state()`는 단기 상태를, `load_learned_parameters()`는 학습된 가중치를 다룬다. DOOMFLY의 연속 방송식 기억 유지 프로토콜은 이 독립 경기 평가와 다르므로 그대로 복사하지 않는다. [S08]

상태를 가진 v7-2에 v6의 ‘같은 관측이면 결과를 재사용’ 규칙을 그대로 적용하면 안 된다. 정지 중 동일한 관측이 이어져도 새로운 게임 tick이면 신경 시간은 진행해야 한다. 반대로 같은 tick에 대한 재호출이면 두 번 진행하면 안 된다. 연구용 어댑터는 다음 키를 요구한다.

```text
(episode_id, env_step_id, team_id, slot_id)
```

현재 공식 제출 경로에 이 신호가 확인되지 않았으므로 연구용 내부 evaluator에는 명시적으로 추가한다. 관측의 남은 시간만으로 모든 초기화·중복 호출 상황을 확실히 구분했다고 주장하지 않는다. 제출 계약이 해결되지 않으면 stateful 전뇌 모델은 제출 후보로 승격하지 않는다. [R05]

모든 시간은 게임의 시뮬레이션 시간으로 진행한다. Unity `timeScale`이나 서버 부하에 따라 신경 dt를 바꾸지 않는다. 처리량이 부족하면 경기가 느려져야지 신경 tick·간선·게임 행동을 몰래 건너뛰면 안 된다. 행동을 여러 스텝 유지하는 최적화는 별도 `action_repeat` 조건으로만 실험한다.

### 5.8 1유닛에서 5유닛으로 확장

첫 실제 게임 단계에서는 한 유닛만 전뇌로 제어하고 나머지 네 유닛·상대·맵은 고정된 비교 조건으로 둔다. `NoOp`, 단순 반응 정책, F0, F1을 같은 슬롯에 번갈아 배치한다. 팀 승률 외에 **해당 슬롯의 실제 이동, 배터리 획득·전달, 정체, 사망 등의 검증 가능한 지표**를 측정한다.

5유닛 단계에서는 희소 배선 배열을 읽기 전용으로 공유하고 막전위·발화·필터 상태는 `[5,N]`으로 독립 유지한다. ‘뉴런 5개=게임 유닛 5개’로 대응하지 않는다. 한 전뇌에서 임의로 다섯 조작을 읽어내는 팀 중앙 제어는 별도의 다른 실험이다.

최종 직접 제어에는 외부 역할 배정기나 목표 계획기를 넣지 않는다. 역할 단서·슬롯 정보를 추가하려면 S1 감각 입력으로 명시하고, 같은 단서를 일반 모델 대조군에도 제공한다. 하나의 초파리 구조에서 팀 협동이 자동으로 나온다고 가정하지 않는다.

### 5.9 v7-2 인과성·학습 검증

| 검증 단계 | 반드시 비교할 것 | 이 단계가 입증하지 않는 것 |
|---|---|---|
| 기술적 폐루프 | 입력 프레임, 신경 상태, 요청 행동, 실제 변위의 시간 정렬 | 게임 이해 |
| 감각 의존성 | 정상 영상 / 감각 차단 / 고정 프레임 / 시간 뒤섞기; 동일 초기 상태·잡음 | 유용한 행동이나 학습 |
| 신경 경로 의존성 | 정상 전뇌 / 출력 집단 차단 / 배선 대조; 우회 입력 감사 | 생물학적 기능의 완전 재현 |
| 출력층 학습 | F1 학습 전후 / 같은 입력의 일반 정책 / 재배선 전뇌+같은 출력층 | 내부 시냅스 학습 |
| 내부 가소성 학습 | F2 / 가소성 정지 / 보상 대응 뒤섞기 / 가소성 가중치만 되돌리기 | 실제 초파리의 의식·이해 |

정상 입력과 다른 영상에서 행동이 달라진다는 사실은 감각이 영향을 준다는 증거일 뿐, 더 잘 행동한다는 증거는 아니다. 별도로 과제 성공률·점수·승률을 비교한다.

F2의 ‘기억 제거’는 학습된 가소성 가중치만 학습 전으로 되돌리는 실험이다. 단순히 막전위와 발화 이력을 초기화하는 실험과 구분한다. 초기 신경 상태는 비교 조건 사이에 동일하게 유지한다. 보상 뒤섞기는 총량·빈도 등 가능한 주변 통계는 보존하되 행동·단서와의 대응을 깨도록 설계하고, 그 방법을 사전에 고정한다.

---

## 6. 구현 파일과 인터페이스 계획

기존 v6 파일을 덮어쓰지 않는다. v7 파일은 아래와 같이 추가하고, 충분한 동치 테스트를 거친 유틸리티만 공통화한다. 아래 경로는 **신규 구현 제안**이다.

```text
blackout_rl/
  connectome/
    datasets.py              # 데이터 등록, 다운로드 manifest, 인증 분리
    graph_artifact.py        # ID·방향·집계·해시·노드/간선 검증
    extract_circuit.py       # v7-1 부분 회로와 입출력 지점
    graph_controls.py       # 배선 재연결·무작위 희소 대조군
    sparse_ops.py           # 희소 계산 및 기준 연산 대조
  v7_1/
    model.py                # V7ResidualModel / GraphResidualHead
    training.py             # 기존 고정 특징 buffer 이후의 PPO
    policy.py               # 기존 계획기+41-way 추론
  v7_2/
    sensory.py              # S0/S1 입력과 좌우·좌표 계약
    dynamics.py             # 고정 전뇌 모델, 상태, 명시적 tick
    decoder.py              # F0 고정 변환 / F1 9-way 출력층
    plasticity.py           # F2 전용; 기본 비활성화
    state.py                # episode·step 키, 초기화, 체크포인트
    policy.py               # 직접 행동; 계획기 호출 금지
    training.py             # F1 PPO / F2 별도 학습 프로토콜
  v7_registry.py            # 모델·환경·상대·예산의 실험 등록

configs/v7/
  common.yaml
  v7_1_flywire.yaml
  v7_1_mlp_control.yaml
  v7_1_rewired.yaml
  v7_2_fixed.yaml
  v7_2_readout_ppo.yaml
  v7_2_plasticity.yaml
  evaluation.yaml

scripts/
  prepare_v7_connectome.py
  audit_v7_connectome.py
  train_v7_1.py
  run_v7_2.py
  evaluate_v7.py
  export_v7.py

tests/v7/
  test_graph_artifact.py
  test_v7_1_contract.py
  test_v7_1_gradients.py
  test_v7_2_dynamics.py
  test_v7_2_causality.py
  test_episode_state.py
  test_evaluation_isolation.py
  test_export_roundtrip.py

reports/v7/
  source_review.md
  data_audit.json
  preregistration.md
  pilot_results.md
  final_results.md
```

`v7_1/training.py`는 v6의 학습·수집 로직을 최대한 재사용하되, 모델 생성·체크포인트·logit 호출을 분리한다. 기존 `V6Model`의 엄격한 로더에 새 구조의 state dict를 억지로 넣지 않는다. `architecture_id`, `graph_sha256`, `observation_schema`, `action_schema`, `dynamics_id`를 검사하는 별도 로더를 둔다. [R02, R03]

v7-1의 인터페이스는 `forward(features[...,5,D]) -> logits[...,41]`다. v7-2는 내부 연구용 API에서 `act(observations, episode_id, env_step_id) -> actions`를 사용한다. 공식 제출용 `forward(vector, graphic)` 어댑터는 상태 전달 문제가 해결된 모델만 제공한다.

데이터 가공 환경에는 필요에 따라 `pyarrow`, `pandas`, `scipy`, CAVE·neuPrint 접근 도구를 둘 수 있다. **추론 의존성에는 자동으로 포함하지 않는다.** v7-1은 우선 기존 `requirements.lock`에 맞는 torch+numpy 경로로 구현한다. 희소 연산의 CPU·CUDA·MPS 지원과 기울기 일치는 실제 사용하는 버전별로 시험하고, 지원되지 않는 장치를 자동 혼합 연산으로 숨기지 않는다.

---

## 7. 실험군과 학습 프로토콜

### 7.1 공통 사전 등록

실험을 시작하기 전에 `preregistration.md`와 기계가 읽는 `experiment_manifest.json`에 다음을 고정한다. 모델, 그래프, 입력 모드, 행동 공간, 보상, 학습 파라미터, 고정 상대, 맵 분할, 난수 시드, 예산, 체크포인트 선택 규칙, 1차 지표와 중단 규칙이다.

초기 인코더와 상대 체크포인트를 이름만으로 식별하지 않는다. 실제 파일 SHA-256을 기록하고, 파일이 없으면 실행을 막는다. ‘win70’ 같은 파일명이 그 상대의 현재 실측 승률을 뜻한다고 가정하지 않는다.

v6 baseline은 두 가지로 구분한다. `v6_frozen_reference`는 실험 시작 시 고정한 정책이고, `v6_retrained_matched`는 v7-1과 같은 학습 예산·조건으로 다시 학습한 비교 정책이다. v7-1 학습 모델과 학습하지 않은 기준만 비교하고 구조 개선을 주장하지 않는다.

### 7.2 v7-1 실험군

| ID | 실험군 | 답하려는 질문 | 우선순위 |
|---|---|---|---|
| A0 | 기존 v6 MLP, 동일 조건 재학습 | 원래 프로젝트보다 유용한가? | 필수 |
| A1 | 총 학습 파라미터 수를 맞춘 MLP | 단순 용량 증가의 효과인가? | 본실험 필수 |
| A2 | 노드별 입·출력 연결 수를 보존해 배선만 바꾼 그래프 | 구체적인 생물학적 배선이 중요한가? | 필수 |
| A3 | 실제 FlyWire 부분 그래프 | 제안 모델 | 필수 |
| A4 | 노드·간선 수를 맞춘 일반 무작위 희소 그래프 | 희소성 자체가 효과적인가? | 후속 |
| A5 | 시냅스 수 초기화 / 고정 가중치 / 부호 제약 | 구조와 추가 생물학적 속성의 기여는? | 후속 |

A2는 A3와 입력·출력 지점, 채널 수, K, 학습층, 초기 가중치 분포를 같게 한다. 방향성 edge swap 이후 중복·자기연결 처리, 연결 차수 보존, 입력→출력 도달성을 감사한다. 차수 보존만으로 가중 연결강도 합계까지 보존되었다고 주장하지 않는다. 필요한 경우 부호별 차수를 보존한 더 엄격한 재배선도 추가한다.

입출력 지점의 차이가 구조 효과로 섞이지 않도록 같은 노드·포트 라벨을 유지한다. 어느 대조군이 입력에서 출력으로 전혀 연결되지 않는다면, 그것을 ‘공정한 무작위 모델’의 대표로 채택하지 말고 해당 구성 문제를 보고한다.

파라미터 수와 계산량을 동시에 완전히 맞추기 어려우므로 두 기준을 별도 보고한다. A1은 학습 파라미터 수를 가능한 ±5% 안으로 맞추고, 별도의 지연 시간 또는 연산량 대응 비교를 추가한다. 고정 연결 수와 학습 파라미터 수를 같은 숫자로 표기하지 않는다.

### 7.3 v7-1 학습 설정

PPO의 초기 설정은 v6 보고서의 `gamma=0.9995`, `gae_lambda=0.99`, `learning_rate=1e-4`, `entropy_coef=0.003`, `target_kl=0.02`, 팀 점수 변화+terminal 보상을 계승한다. 실제 실행 시 설정 파일 전체를 고정하고, 본문 숫자만으로 나머지 설정을 추측하지 않는다. [R04]

구조 비교용 주 실험에서는 성과에 따라 바뀌는 커리큘럼·실패 시드 우선 표집을 끈 **고정된 비교 일정**을 사용한다. 이는 v6의 실전 운영 기능을 없애자는 뜻이 아니라 모델마다 다른 경험 분포가 구조 비교에 섞이지 않게 하려는 조치다.

| 누적 환경 스텝 | scripted / weak / target 상대 비율 | 공통 조건 |
|---|---|---|
| 0~200,000 | 65% / 25% / 10% | 동일 상대 선택 규칙, 동일 맵 목록 |
| 200,000~600,000 | 20% / 30% / 50% | 성과와 무관하게 동일 스텝에서 전환 |
| 600,000~2,000,000 | 10% / 0% / 90% | 동일 최종 학습 예산 |

A/B 학습 노출 비율도 모든 실험군에서 동일하게 유지한다. 초기 제안은 v6에 맞춘 A 40%, B 60%다. 평가에서는 양 진영을 동등하게 사용한다.

이 일정은 인과 비교를 위한 제안이며 v6의 원래 승급 조건과 다르다. 비교가 끝난 뒤 우수 모델을 기존 adaptive curriculum·failure sampler·rollback과 결합하는 것은 별도의 운영 성능 실험으로 등록한다. 이때 기존 MLP에도 동일한 운영 기능을 적용한다.

한 모델만 행동 모방이나 추가 지도 데이터를 사용하지 않는다. 사전학습을 도입하면 같은 데이터·동일 예산의 대조군을 추가하고 비용을 포함한다. 파일에 저장된 과거 PPO transition을 새 정책의 on-policy 데이터처럼 재사용하지 않는다.

### 7.4 v7-2 실험군

| ID | 실험군 | 역할 |
|---|---|---|
| B0 | F0: 고정 전뇌+고정 변환기 | 무학습 폐루프 기준 |
| B1 | F1: 고정 전뇌+PPO 출력층 | 전뇌 특징을 이용한 게임 학습 |
| B2 | 재배선 전뇌+동일 PPO 출력층 | 실제 전뇌 배선의 추가 가치 |
| B3 | 동일 영상·행동·시간 조건의 CNN/GRU 정책 | 감각 인터페이스를 같게 한 일반 학습 기준 |
| B4 | F2: 일부 연결 가소성+고정 출력층 | 내부 가소성에 의한 적응 |
| B5 | B4와 동일하되 가소성 비활성 / 보상 대응 뒤섞기 | 학습 인과성 대조 |

각 실험명에 `S0/S1/S2`, `one_unit/five_units`, `dynamics_id`, `action_repeat`, `plasticity_on/off`를 포함한다. 다른 입력 정보나 유닛 수를 가진 결과를 한 학습 곡선에 합치지 않는다.

B1과 B3에는 같은 센서 영상, 같은 합법적 보조 입력, 같은 9-way 행동, 같은 상대·맵·보상을 제공한다. B3에만 원래 96차원 벡터 전체를 주거나, B1에만 계획기 힌트를 주지 않는다. 필요하면 동일 최근 영상 이력을 입력받는 CNN과 시간적 GRU를 함께 비교해 기억 용량의 차이도 살핀다.

### 7.5 v7-2 과제 단계

**T0: 오프라인 감각·신경·출력 테스트.** 밝기 변화, 좌우 변화, 이동 패턴과 실제 train 관측의 기록 영상을 사용한다. 그래프 개입과 잡음 분리를 확인한다. 합성 영상 반응만으로 실제 게임 성공을 주장하지 않는다.

**T1: 실제 게임 한 유닛 제어.** 같은 슬롯을 B0/B1/대조군으로 바꾸고 나머지 팀 구성과 상대를 고정한다. 특정 슬롯의 탐색·배터리 회수·정체를 측정한다. 팀 승률만으로 해당 슬롯의 기여를 판단하지 않는다.

**T2: 한 유닛의 목적 있는 행동.** 배터리 회수 후 창고 전달, 장애물 때문에 돌아가야 하는 이동, 적 접근 시 손실 억제 같은 상황을 train 맵에서 평가한다. 환경이 임의 위치 초기화를 지원한다고 가정하지 않는다. 지원하지 않으면 실제 초기 상태에서 동일한 준비 행동을 실행해 상황을 만들고, 준비 구간·개입 시점·난수를 기록한다. Unity 상태를 몰래 수정한 합성 과제와 공식 경기를 구분한다.

**T3: 다섯 유닛 직접 제어.** 한 유닛의 감각 의존성과 학습 신호가 확인된 경우에만 확장한다. 목표 중복, 역할 쏠림, 한 행동으로 수렴하는 현상과 계산 비용을 함께 측정한다.

미니과제에 보상 형성이 필요하면 기존 점수 보상과 분리한 실험으로 등록한다. 예를 들어 관측에서 계산 가능한 제한된 거리 잠재함수로 보상할 수 있지만, 같은 보상을 모든 비교 모델에 제공하고 최종 경기는 원래 점수·승패로 평가한다. 위치별 보상이나 준비 행동이 숨겨진 계획기로 작동하지 않는지 감사한다.

---

## 8. 평가 설계와 결과 판정

### 8.1 데이터 분할

학습용 맵, 개발용 맵, 반복 선택용 확인 맵, 최종 테스트 맵을 구분한다. 기존 v6 분할은 회귀 확인에 남긴다. v7 신규 후보 분할은 다음과 같이 제안하되 **기존 전체 실험 이력과의 겹침 검사 후 확정**한다.

```text
train:        기존 v6 train 목록에서 시작; 실제 목록을 manifest로 고정
v7_dev:       41000~41029   (30개)
v7_confirm:   42000~42049   (50개)
v7_test:      43000~43099   (100개)
```

숫자 범위 자체가 미사용을 보장하지 않는다. 이전 run·로그·체크포인트 선택 이력에 등장하면 새 목록을 생성한다. 최종 test 목록과 평가 스크립트 해시를 먼저 고정하고, 성능 결과는 모델·설정·체크포인트 선택을 완료한 뒤 한 번 공개한다.

v6의 기존 confirmation도 반복 모델 선택에 사용되는 집합이므로 최종 test가 아니다. v7에서도 같은 원칙을 지킨다. [R04]

### 8.2 난수와 상대 통제

예비 실험은 3개, 본실험은 최소 5개 독립 학습 시드를 제안한다. 이 수는 성능 입증을 보장하는 마법의 기준이 아니다. 파일럿에서 학습 시드와 맵별 변동성을 확인한 뒤 필요한 표본을 사전 조정한다.

각 평가 맵은 A/B 진영을 바꿔 두 번 실행한다. 상대는 고정 체크포인트 SHA로 구분하고 scripted·목표 상대·개발에 사용하지 않은 보유 상대를 별도로 보고한다. 상대 풀을 보고 고른 모델은 그 풀에 대한 완전한 미관측 일반화를 주장하지 않는다.

난수는 학습 초기화, 정책 표집, 맵 순서, 상대 선택, 전뇌 배경 잡음을 분리한다. 동일한 그래프를 공유하는 여러 run은 서로 독립적인 뇌 구조 표본이 아니라 같은 개체에서 파생한 계산 실험임을 명시한다.

### 8.3 지표

1차 성능 지표는 실제 terminal 승률 `W/(W+D+L)`와 기준 정책 대비 승률 차이다. 무승부율과 점수 차를 별도 보고하고, `승=1·무=0.5·패=0` 점수를 사용하면 ‘승률’과 이름을 구분한다.

보조 지표는 평균·중앙 최종 점수 차, A/B별 결과, 목표 도달까지의 환경 스텝, 학습 곡선 면적, 전체 학습 처리량, 추론 지연의 중앙값·95백분위, 최대 메모리다. 학습 곡선은 강한 계획기 덕분에 처음부터 높을 수 있으므로 초기 성능 대비 개선도 함께 표시한다.

v7-1에는 KEEP·보정 빈도, team-step과 agent-action 기준의 보정률, 보정 가능한 상태 비율, 계획기 예외, 정체 시간, 행동 왕복, 점수 흡수 전후 결과를 추가한다.

v7-2에는 감각 프레임 수, 실제 신경 tick 수, 시뮬레이션 시간/실제 시간 비율, 입력·출력 집단 활동, 발화 폭주·침묵, 감각 개입에 대한 출력 차이, 요청 행동과 실제 변위, 학습된 파라미터 범위, 상태 초기화 횟수를 추가한다. 발화율이나 바뀐 가중치 개수 자체를 게임 성능 지표로 사용하지 않는다.

### 8.4 통계 단위

같은 맵의 A/B 두 경기를 완전히 독립인 두 표본으로 처리하지 않는다. 모델별·학습 시드별로 맵의 두 결과를 묶고, 같은 맵·상대 조건의 비교 모델과 차이를 계산한다.

신뢰구간은 학습 시드와 맵을 함께 고려하는 재표집으로 계산한다. 예를 들어 학습 시드와 맵 ID를 각각 재표집하되 A/B 쌍은 유지하는 방식이다. 서로 다른 상대는 우선 별도 층으로 보고한다. 독립 학습 시드 수가 작을 때는 불확실성이 큰 상태로 남을 수 있음을 명시한다.

생물학적 배선 효과의 사전 지정 비교는 `A3 vs A1`, `A3 vs A2`다. 여러 가설에 대해 유의성을 주장하면 다중 비교 보정을 적용한다. 보고서에는 효과 크기와 신뢰구간을 우선 제시하며, 가장 유리한 한 시드나 한 상대만 선택하지 않는다.

최종 테스트가 100개 맵×양 진영×5개 학습 시드라면 **모델당·상대당 1,000경기**다. 같은 맵에서 반복한 경기 수를 새로운 독립 맵 수처럼 세지 않는다. 5%p 차이를 검출하기에 충분한지는 이 숫자만으로 확정하지 않는다.

### 8.5 승격 기준

**v7-1 실용적 승격 제안:** 동일 학습 예산의 A0 대비 평균 승률 차이 +5%p 이상, 불확실성 평가상 유의미한 열화가 아닌 개선, 특정 진영의 큰 회귀 없음, 전체 처리량 감소가 내부 허용 범위 이내일 때 후보로 둔다. 초기 처리량 한도는 기준 대비 25% 이상 악화하지 않는 것으로 제안한다. 공식 추론 한도가 있으면 그 조건이 우선이다.

**v7-1 구조적 연구 성과:** A1 및 A2와 비교해 개선이 반복되어야 ‘FlyWire 특유의 배선이 기여했다’고 해석한다. A0만 이기고 A1/A2와 비슷하면 모델 크기·희소성·학습 설정의 효과일 수 있다.

**v7-2 제어 성공:** 정상 감각 입력이 신경 활동과 행동에 인과적으로 영향을 주고, 행동 요청이 실제 게임 동작으로 이어지며, 재생·초기화 검증을 통과한다. 이는 학습 성공과 별개다.

**v7-2 학습 성공:** B1은 학습 전 및 같은 감각 조건의 대조군 대비 미관측 과제에서 개선을 보여야 한다. B4는 가소성 정지·보상 대응 대조보다 개선되고, 가소성 가중치 복원 개입으로 그 개선의 원인을 설명할 수 있어야 한다. 단순히 게임을 오래 켜두었거나 가중치가 변했다는 사실은 기준을 충족하지 않는다.

최종 승률이 기존 프로젝트보다 낮아도 유용한 감각·학습 기전을 확인했다면 연구 결과로 남길 수 있다. 반대로 재미있는 영상만 있고 대조 실험이 없으면 데모로 분류한다.

### 8.6 중단·재설계 기준

데이터 ID·방향·수치 검증이 실패하면 모든 학습을 중지한다. v7-1에 기울기가 전달되지 않거나 업데이트 전 PPO 비율이 1이 아니면 성능 실험을 시작하지 않는다.

v7-2의 입력 차단·출력 차단에도 동일하게 의미 있는 행동이 유지된다면 배경 구동 또는 우회 경로를 먼저 조사한다. 감각 영향이 거의 없으면 입력·동역학·출력 대응을 재검토하고, 내부 가소성 학습으로 문제를 덮지 않는다.

반복 검증에서 NaN·발화 폭주·상태 누출이 나타나면 해당 run을 실패로 기록한다. 학습이 붕괴한 run을 통계에서 삭제하거나 좋은 시드로 교체하지 않는다. 지연·메모리 때문에 전체 그래프 실행이 불가능하면 그 하드웨어 조건에서의 결과를 기록하고, 축소 모델을 전뇌 결과로 대체하지 않는다.

성능 차이의 신뢰구간이 넓으면 ‘효과 없음’이 아니라 ‘이번 예산에서 판정 불충분’으로 기록한다. 실제로 성능·비용이 모두 열세라면 해당 구성은 실용 후보에서 제외한다.

---

## 9. 계산 비용과 단계별 예산

### 9.1 먼저 측정할 것

전체 pipeline에서 환경 step, 의미 지도 렌더링, 인코더, 계획기, 그래프 계산, 출력층, PPO 업데이트, 평가·저장 시간이 각각 차지하는 비율을 측정한다. 신경 모듈만 빠르게 만들어도 Unity나 계획기가 병목이면 전체 학습은 빨라지지 않는다.

실행 시간은 다음 측정값으로 산정한다. 아래는 일정 약속이 아니라 예산 산정식이다.

```text
예상 총 실행 시간
  = 학습 환경 스텝 / 측정된 전체 학습 steps_per_second
  + 평가 경기 수 × 측정된 경기 시간
  + 데이터 준비·검증·저장 비용
```

### 9.2 v7-1 제안 예산

| 단계 | 구성 | 환경 스텝 예산 | 해석 |
|---|---|---:|---|
| 연결·기울기 점검 | 합성 테스트와 소규모 실제 수집 | 별도 짧은 실행 | 성능 판정에 사용하지 않음 |
| 파일럿 | A0/A2/A3 × 3시드 × 200,000 | 1,800,000 | 안정성·처리량·초기 학습 신호 |
| 본실험 | A0/A1/A2/A3 × 5시드 × 2,000,000 | 40,000,000 | 구조·용량·실용성 비교 |
| 후속 | A4/A5, 일반화 확장 | 결과와 비용을 보고 별도 등록 | 최초 실험과 구분 |

평가 경기는 이 학습 예산에 포함하지 않고 별도로 계산한다. 위 본실험 규모를 자동으로 실행하지 않는다. 파일럿으로 실제 처리량과 경기 비용을 구한 뒤 자원 예산을 확정한다. 예산을 줄여야 하면 결론의 범위를 좁히고, 실패 run을 골라 빼는 방식으로 줄이지 않는다.

### 9.3 v7-2 메모리와 속도

참조 구현의 유지 간선 수 `E=25,582,938`을 사용할 때, 간선 하나당 int32 인덱스 하나와 float32 가중치 하나만 저장하는 단순 CSR 핵심 배열은 약 **205MB**이고 행 포인터 등이 추가된다. 이는 배열 크기의 산술 추정일 뿐 전체 프로세스 메모리가 아니다. 반대 방향 인덱스, 원본 로딩, 캐시, 감각·지연·가소성·신경 상태가 더 필요하다. [S06]

5개 인스턴스는 동일한 고정 그래프를 공유할 수 있지만 상태와 계산을 공유하는 것은 아니다. 한 번의 float32 노드 상태 배열은 `5×166,700×4` 바이트, 약 3.3MB지만 여러 상태와 발화 이력, 지연 큐가 붙는다. 내부 가중치를 인스턴스별로 학습하면 가변 가중치 메모리도 증가한다.

전뇌 전체 시간열에 대한 역전파를 최초 목표로 삼지 않는다. F1은 고정 전뇌 특징만 저장하고 작은 출력층을 학습하므로 그래프 역전파 비용을 피한다. F2는 제한된 실제 연결에 대한 활동 흔적과 가변 효과만 저장한다.

첫 성능 파일럿은 **한 유닛, B1/B2/B3, 3개 시드, 각 128,000 환경 스텝**을 제안한다. 합계는 1,152,000 환경 스텝이며 B0의 무학습 평가와 각종 개입 시험은 별도다. 이 예산은 학습 성공을 보장하지 않는다. 5유닛·장기 본실험과 F2는 인과성 및 처리량 검증을 통과한 뒤 별도로 등록한다.

### 9.4 저장 비용

전체 뉴런 발화를 모든 tick마다 float 배열로 기록하지 않는다. 일상 로그는 선택 집단의 활동·상태 요약·행동·시간을 저장하고, 정밀 재생용 짧은 구간만 손실 없는 압축 또는 bit packing을 사용한다. 감각 입력·RNG·초기 상태를 보관해 같은 코드·장치에서 재생 가능성을 검증한다.

상세 replay의 보관 정책을 사전에 고정한다. 실패 구간도 남긴다. 보기 좋은 성공 영상만 기록하면 실패 분석과 인과성 검증이 불가능해진다.

---

## 10. 구현 작업표와 완료 조건

작업은 날짜 약속 대신 의존 관계와 산출물로 관리한다. 공통 기반이 끝나면 v7-1과 v7-2의 개발은 분리할 수 있지만, 최종 비교 전에 동일한 평가 계약을 사용해야 한다.

| ID | 작업 | 선행 조건 | 완료 산출물·검증 |
|---|---|---|---|
| C-01 | 기준 소스·환경·인코더·상대·기존 결과 고정 | 없음 | source/config/build/checkpoint 해시; 미측정 상태 명시 |
| C-02 | 데이터 취득·라이선스·주석 버전 고정 | C-01 | 인증정보 없는 source manifest, 사용 조건 기록 |
| C-03 | 그래프 변환기·독립 감사 구현 | C-02 | 원본 ID·방향·집계·포함/제외 수 일치 보고 |
| C-04 | v7 분할·지표·통계 사전 등록 | C-01 | 겹침 없는 split manifest, 테스트 잠금 |
| A-01 | 부분 회로와 입출력 선정 | C-03 | 실제 node ID 목록, 경계 손실, 도달성 보고 |
| A-02 | 기존 MLP의 v7 래퍼 동치 검증 | C-01 | v6와 logits/action/log-prob 일치 |
| A-03 | GraphResidualHead·저장/복원 구현 | A-01, A-02 | 희소 계산·기울기·초기 KEEP·PPO 비율 테스트 |
| A-04 | A0/A2/A3 파일럿 | A-03, C-04 | 모든 시드 결과·실패·처리량·기여도 기록 |
| A-05 | A1 포함 본실험·블라인드 test | A-04 | 사전 지정 비교, 신뢰구간, 최종 판정 |
| B-01 | 전뇌 상태·동역학·참조 연산 검증 | C-03 | 작은 회로의 정확성, 전뇌 수치 안정성 |
| B-02 | 감각·출력 어댑터와 step 계약 | B-01 | 좌우·시야·행동·시간 정렬·중복 호출 시험 |
| B-03 | F0 실제 게임 폐루프·개입 시험 | B-02, C-04 | 입력·출력 차단 대조, 한 유닛 실제 변위 |
| B-04 | F1 출력층 PPO와 대조군 | B-03 | 고정 전뇌 검증, 학습 전후·B2/B3 비교 |
| B-05 | 5유닛 직접 제어 | B-04 | 독립 신경 상태, 무계획기 행동 경로, 협동 지표 |
| B-06 | F2 국소 가소성 연구 | B-03 및 사전 등록된 출력층 | 무가소성·보상 대조·가중치 복원 시험 |
| C-05 | 독립 제출 패키지와 재현 보고 | 해당 경로 성공 및 공식 계약 확인 | 두 파일 로딩, 네트워크 차단, 다경기 격리, 자원 한도 |

**우선 개발 순서:** C-01~C-04 → A-01~A-04 → B-01~B-03 → 파일럿 비용에 근거한 A-05/B-04 선택 → 5유닛·가소성·제출 확장. 두 옵션을 모두 설계하되, 가장 비싼 전뇌 학습부터 시작하지 않는다.

---

## 11. 설정·실행 명세와 산출물

### 11.1 v7-1 설정 예시

다음 YAML은 신규 설정 로더에 구현할 계약이며 현재 저장소에서 바로 실행되는 파일이 아니다. `null` 필드는 실제 데이터 감사와 체크포인트 확인으로 채워지기 전 실행을 차단해야 한다.

```yaml
schema_version: blackout.v7.experiment.v1
experiment_id: v7_1_fafb783_topology
base_commit: 2842fb17aeb6923f55c391d04de182df758f02e3
mode: residual_graph

data:
  dataset: flywire_fafb
  snapshot: 783
  graph_artifact: data/connectomes/fafb783/cx_primary.npz
  graph_sha256: null
  annotation_sha256: null
  source_manifest: data/connectomes/fafb783/source_manifest.json

model:
  architecture_id: blackout.v7_1.graph_residual.v1
  frozen_encoder_checkpoint: null
  frozen_encoder_sha256: null
  input_context_dim: 49
  slots: 5
  node_channels: 4
  input_port_count: 16
  output_pool_count: 16
  graph_rounds: 4
  leak: 0.5
  graph_weight_mode: trainable_topology_only
  carry_neural_state_between_calls: false
  direct_feature_to_action_bypass: false
  keep_logit: 0.0
  initial_override_probability: 0.10
  action_schema: keep_plus_5x8_v6

training:
  protocol: fixed_comparison_schedule_v1
  reward: inherited_v6_score_delta_terminal
  gamma: 0.9995
  gae_lambda: 0.99
  learning_rate: 0.0001
  entropy_coef: 0.003
  target_kl: 0.02
  failure_seed_priority: false
  max_environment_steps: 2000000
  independent_training_seeds: [11, 22, 33, 44, 55]
  remaining_ppo_config: inherit_and_freeze_v6

evaluation:
  split_manifest: configs/v7/splits.json
  paired_sides: true
  terminal_winner_only: true
  final_test_after_model_lock: true
  opponents_manifest: null
```

노드 수나 실제 출력 지점 때문에 제안 포트 수를 조정하면 확정 config에 반영하고 모든 대응 대조군을 갱신한다. 가짜 포트를 만들어 형태만 맞추지 않는다. 상속 설정은 실행 시 완전히 펼친 `resolved_config.json`으로 저장해 숨은 기본값을 없앤다.

### 11.2 v7-2 설정 예시

```yaml
schema_version: blackout.v7.experiment.v1
experiment_id: v7_2_malecns_f1_s0_one_unit
base_commit: 2842fb17aeb6923f55c391d04de182df758f02e3
mode: whole_connectome_direct

data:
  dataset: male_cns
  release: v1.0
  inclusion_policy: annotated_neurons_reference_v1
  source_manifest: data/connectomes/malecns_v1/source_manifest.json
  graph_sha256: null
  runtime_pruning: false

sensory:
  mode: S0_semantic_retina
  retinal_mapping_manifest: null
  rendered_width: 128
  rendered_height: 64
  horizontal_fov_degrees: 270
  sensor_interval_game_steps: 5
  extra_vector_to_decoder: false
  planner_target_overlay: false

brain:
  dynamics_id: fly64_proxy_v1
  dynamics_config_sha256: null
  integration_dt_seconds: 0.02
  input_to_hidden_trainable: false
  recurrent_weights_trainable: false
  shared_readonly_graph: true
  independent_state_per_slot: true
  noise_seed_separate_from_policy_seed: true

controller:
  training_mode: F1_readout_ppo
  action_schema: noop_plus_8_directions
  action_repeat: 1
  controlled_slots: [0]
  remaining_team_policy: fixed_scripted_reference
  remaining_team_policy_sha256: null
  planner_fallback_for_controlled_slots: false
  plasticity_enabled: false

state:
  require_episode_id: true
  require_env_step_id: true
  reset_fast_state_each_episode: true
  freeze_learning_during_evaluation: true
  share_mutable_state_across_slots: false

training:
  reward: inherited_v6_score_delta_terminal
  max_environment_steps: 128000
  independent_training_seeds: [11, 22, 33]
  opponents_manifest: null
```

`fly64_proxy_v1`과 0.1ms LIF 설정은 서로 교환 가능한 이름이 아니다. 다른 동역학은 별도 설정 파일·검증 결과·실험 ID를 갖는다. F2를 켜려면 가소성 규칙·변경 가능한 실제 간선·보상 타이밍·평가 동결 정책이 채워진 별도 파일이 필요하다.

### 11.3 CLI 계약

아래는 **구현 완료 후 제공할 명령 형태**다. 이 문서 작성 시점에 아래 새 스크립트가 생성되거나 실행된 것은 아니다.

```bash
# 원본 취득·고정. 인증 토큰은 환경변수로만 주입한다.
python scripts/prepare_v7_connectome.py --config configs/v7/v7_1_flywire.yaml
python scripts/audit_v7_connectome.py --config configs/v7/v7_1_flywire.yaml

# --check는 Unity 학습과 산출물 덮어쓰기 없이 계약을 검사한다.
python scripts/train_v7_1.py --config configs/v7/v7_1_flywire.yaml --check
python scripts/train_v7_1.py --config configs/v7/v7_1_flywire.yaml --seed 11

# 전뇌는 고정 제어와 출력층 학습을 별도 설정으로 실행한다.
python scripts/run_v7_2.py --config configs/v7/v7_2_fixed.yaml --check
python scripts/run_v7_2.py --config configs/v7/v7_2_fixed.yaml --seed 11
python scripts/run_v7_2.py --config configs/v7/v7_2_readout_ppo.yaml --seed 11

# 최종 test는 모델 잠금 manifest가 있어야 허용한다.
python scripts/evaluate_v7.py --run RUN_DIR --split dev
python scripts/evaluate_v7.py --run RUN_DIR --split test --lock MODEL_LOCK

# 공식 제출 계약과 독립 로딩 검사를 통과한 모델만 export한다.
python scripts/export_v7.py --run RUN_DIR --output submission/v7_candidate
```

여러 시드 실행은 외부 실행 관리기가 담당한다. 다른 run의 디렉터리·체크포인트를 공유하지 않고 lock과 중복 실행 검사를 둔다. 데이터가 없는데 `--check`를 통과시키거나 시험용 그래프를 실제 데이터로 표시하지 않는다.

### 11.4 로그와 체크포인트

```text
logs/v7/<experiment_id>/<seed>/
  source_manifest.json
  resolved_config.json
  experiment_manifest.json
  graph_audit.json
  runtime_info.json
  training.jsonl
  episodes.jsonl
  eval_dev_*.json
  eval_confirmation_*.json
  diagnostics.jsonl
  checkpoints/
  replay_samples/
  final_model_lock.json
  final_test.json
  result_summary.md
```

로그에는 환경 스텝과 유닛 행동 수, 신경 tick 수를 각각 기록한다. `action_repeat>1`이면 한 의사결정이 소비한 실제 게임 스텝을 표시한다. 반복 구간 보상은 원래 할인율을 반영해 합산하고, bootstrap 할인은 `gamma^k`로 처리한다. 원래 시간 척도를 보존할 GAE 계수도 명시적으로 설계한다. 단순히 기록 스텝 수를 줄여 샘플 효율이 좋아졌다고 계산하지 않는다.

v7-1 체크포인트에는 모델·옵티마이저·RNG·그래프/소스/설정 해시를 넣는다. v7-2에는 선택한 모델의 단기 신경 상태, 가변 시냅스, 감각·출력 필터, 지연 이벤트, 가상 방향까지 필요한 항목을 분리해 저장한다.

Unity 물리 상태까지 저장하지 못한 중단 재개에서는 새로운 경기로 시작하고 폐기된 진행 경기·pending 보상을 기록한다. 신경 상태 파일을 읽었다는 이유로 중단 전 경기와 완전히 동일하게 이어진다고 주장하지 않는다. 평가용 재시작과 훈련 재개의 초기화 정책도 구분한다.

### 11.5 제출 패키지 검증

`policy.py`와 `checkpoint.pt`만 존재하는 격리 환경에서 로드되는지, 학습 작업 폴더 없이 추론 가능한지, 네트워크를 차단해도 동작하는지 확인한다. 그래프는 검증된 체크포인트 텐서·버퍼로 포함하거나 공식 허용 형식으로 함께 제공한다. 별도 대용량 파일을 몰래 참조하지 않는다.

다경기·다팀 인스턴스를 반복 실행해 상태가 섞이지 않는지 확인한다. 데이터·코드 귀속 문구를 포함한다. v7-2에 필요한 초기화·step ID·추가 연산 라이브러리가 공식 계약에서 지원되지 않으면 연구 결과만 배포하고 제출 가능 표시를 하지 않는다.

전뇌 정책을 나중에 작은 stateless 정책으로 증류할 수는 있지만, 그 제출물은 ‘전뇌 모형을 추론에 사용하는 정책’이 아니라 ‘전뇌 교사의 행동을 학습한 별도 정책’이다. 증류 결과는 새로운 실험 ID와 추가 학습 비용을 가진다.

---

## 12. 결과 해석과 최종 선택 원칙

**v7-1이 일반 MLP와 재배선 대조군 모두를 이긴다:** 해당 회로·입출력·과제 범위에서 실제 배선이 유용한 구조적 제약이라는 근거다. 다른 뇌 영역이나 다른 게임 전체로 일반화한 결론은 아니다.

**v7-1이 기존 MLP만 이기고 규모를 맞춘 MLP·재배선 모델과 비슷하다:** 실용 개선이 있을 수 있으나 그 원인을 FlyWire 특유의 배선으로 단정하지 않는다. 더 단순하고 빠른 모델을 채택할 수 있다.

**v7-2가 화면에 반응하며 움직이지만 성과가 없다:** 폐루프 구현은 성공했지만 목적 있는 제어나 학습 성과는 확인되지 않았다. 감각 인터페이스, 동역학, 읽기 지점, 과제 부적합을 순서대로 분석한다.

**v7-2 출력층 PPO만 개선된다:** 고정된 전뇌 활동이 학습 가능한 특징으로 사용됐다는 결과다. 내부 시냅스가 게임을 학습했다는 표현은 사용하지 않는다.

**v7-2의 내부 가소성이 대조 실험까지 통과한다:** 명시한 인공 감각·보상·동역학 조건에서 연결 효과의 변화가 행동 개선에 기여했다는 결과다. 살아 있는 초파리의 학습·의식·게임 이해가 재현됐다는 결론으로 확장하지 않는다.

**두 모델 모두 개선이 없다:** 실패 run, 대조군, 데이터 감사, 계산 비용을 함께 공개해 어떤 가설을 어떤 조건에서 검증했는지 남긴다. 한 구성의 실패가 모든 커넥톰 기반 접근을 부정하지는 않지만, 대회 성능을 위해 계속 사용할 근거가 없으면 기존 정책을 유지한다.

현재 프로젝트에 대한 권고는 **v7-1의 작은 변경으로 성능 개선 가능성을 먼저 측정하고, v7-2에서는 무학습 제어 재현→감각 인과성→출력층 학습→팀 확장→내부 가소성의 증거 단계를 지키는 것**이다. 전체 뇌를 넣었다는 사실보다 어떤 정보가 어떤 계산을 거쳐 실제 성과로 이어졌는지가 최종 평가 대상이다.

---

## 13. 참고자료와 검토 범위

모든 웹·저장소 자료의 확인일은 **2026-09-14**다. 공개 프로젝트는 이후 바뀔 수 있으므로 아래 GitHub 주소는 가능한 한 확인한 커밋으로 고정했다. 자료의 주장과 이 계획의 제안·가설을 구분해 인용한다. 인증이 필요한 데이터 원본과 Unity 실행 환경은 이 문서 작성 과정에서 검증하지 않았다.

### 프로젝트 근거

**[R01] 2026-IST-tech-RL — README 및 기준 커밋**  
확인 사항: v6 상태, 기존 파이프라인, 실제 학습·목표 성능의 미보고.  
`https://github.com/thislis/2026-IST-tech-RL/blob/2842fb17aeb6923f55c391d04de182df758f02e3/README.md`

**[R02] `blackout_rl/mappo_v6.py`**  
확인 사항: 고정 인코더, 49차원 문맥, 41-way 팀 행동, 한 유닛 보정, 좌표와 모델 로딩.  
`https://github.com/thislis/2026-IST-tech-RL/blob/2842fb17aeb6923f55c391d04de182df758f02e3/blackout_rl/mappo_v6.py`

**[R03] `blackout_rl/mappo_v6_training.py`**  
확인 사항: detached 특징 저장, 실제 행동분포 재평가, 팀 단위 PPO, 에피소드 처리.  
`https://github.com/thislis/2026-IST-tech-RL/blob/2842fb17aeb6923f55c391d04de182df758f02e3/blackout_rl/mappo_v6_training.py`

**[R04] MAPPO planner residual v6 — 구현 및 실행 안내**  
작성일: 2026-09-08. 확인 사항: 이전 실패 원인, PPO 설정, 커리큘럼, 검증 범위, split·confirmation·test 구분.  
`https://github.com/thislis/2026-IST-tech-RL/blob/2842fb17aeb6923f55c391d04de182df758f02e3/reports/mappo_planner_residual_v6_plan_changes.md`

**[R05] PREP-13 평가 계약 확인 목록**  
확인 사항: 제출 형식과 입력 형태, stateless·reset·배치·자원·의존성의 미확인 항목. 이 문서는 고정 upstream revision에 대한 검토이며 최신 대회 공식 답변을 대체하지 않는다.  
`https://github.com/thislis/2026-IST-tech-RL/blob/2842fb17aeb6923f55c391d04de182df758f02e3/reports/prep13_evaluation_contract_checklist.md`

**[R06] PREP-07 Action Semantics Report**  
확인 사항: 비영벡터 정규화, 정지, 20ms 물리 스텝에 대한 측정.  
`https://github.com/thislis/2026-IST-tech-RL/blob/2842fb17aeb6923f55c391d04de182df758f02e3/reports/prep07_action_semantics.md`

**[R07] `blackout_rl/observation.py`**  
확인 사항: 96차원 벡터 필드와 11채널 의미 지도.  
`https://github.com/thislis/2026-IST-tech-RL/blob/2842fb17aeb6923f55c391d04de182df758f02e3/blackout_rl/observation.py`

### 공식 데이터·참조 구현

**[S01] FlyWire 공식 홈페이지**  
용도: FAFB의 성체 암컷 뇌 데이터라는 성격과 대표 공개 규모. 실행 가능한 게임 AI 제공을 뜻하지 않는다.  
`https://flywire.ai/`

**[S02] Codex 공식 FAQ**  
용도: 데이터셋·스냅샷 구분, 정적 다운로드, 토큰, 업데이트되는 주석, 연결 방향과 임계값·영역별 행. 검색 결과의 오래된 캐시보다 직접 열람한 설명을 우선했다.  
`https://codex.flywire.ai/faq`

**[S03] FlyWire 공식 인용·공개 데이터 가이드**  
용도: FAFB 공개 데이터 버전, CC BY-NC 4.0 및 귀속 확인. 여기의 FAFB 설명을 다른 프로젝트 데이터의 이용 조건으로 확장하지 않는다.  
`https://flywire.ai/guidelines`  
보조 자료: `https://flywire.ai/tos`

**[S04] MaleCNS 공식 데이터 다운로드**  
용도: v1.0 원본 파일, Feather 형식, 프로그램 접근 및 CC-BY 표시.  
`https://male-cns.janelia.org/download/`

**[S05] Fly64 — 저자 README**  
확인 커밋: `f2f4114e53eaa326e54129f27a5383f93c6957af`. 용도: 게임 감각→전뇌→조작 데모, 무학습·무보상이라는 저자의 명시.  
`https://github.com/ornata/fly/blob/f2f4114e53eaa326e54129f27a5383f93c6957af/README.md`

**[S06] Fly64 — Technical notes**  
용도: 포함 그래프, 감각 대응, 동역학·주기·발화 출력·재생·검증의 구체적인 공학 설계와 한계. 이 문서에 기재된 테스트 결과는 저자의 보고이며 이번 작업에서 재실행하지 않았다.  
`https://github.com/ornata/fly/blob/f2f4114e53eaa326e54129f27a5383f93c6957af/docs/technical-notes.md`

**[S07] DOOMFLY — 저자 README**  
확인 커밋: `71ecf53d78eaffaf1a57ed7b0ccf5d458abc9f33`. 용도: 현재 실험 개요, 학습 성공 미입증 상태, 배선·감각·조작·가소성 구분.  
`https://github.com/nftechie/doomfly/blob/71ecf53d78eaffaf1a57ed7b0ccf5d458abc9f33/README.md`

**[S08] DOOMFLY — Live experimental training**  
문서 날짜: 2026-09-05. 용도: 제한된 연결 가소성, 실제 실행과 과학적 검증 실패의 구분, 연속 실행과 독립 평가의 차이.  
`https://github.com/nftechie/doomfly/blob/71ecf53d78eaffaf1a57ed7b0ccf5d458abc9f33/docs/doom-live-training.md`

**[S09] DOOMFLY — Neuroscience and implementation review**  
검토 날짜: 2026-09-05. 용도: 데이터 포함 정책, 모델 가정, 감각 차단 대조, 수치 검증의 중요성. 과거 고정 모델의 설명과 이후 가소성 실험을 구분했다.  
`https://github.com/nftechie/doomfly/blob/71ecf53d78eaffaf1a57ed7b0ccf5d458abc9f33/docs/doom-neuroscience-review.md`

### 연구 논문

**[S10] Shiu et al. (2024), “A Drosophila computational brain model reveals sensorimotor processing”, Nature 634, 210–219**  
DOI: `10.1038/s41586-024-07763-9`. 용도: 커넥톰 기반 LIF 모델, 섭식·그루밍 검증, 신경전달물질·동역학·신경조절의 한계. 게임 학습 근거로 사용하지 않았다. 공개된 논문 HTML을 열람했다.  
`https://pmc.ncbi.nlm.nih.gov/articles/PMC11446845/`

**[S11] Hulse et al. (2021), “A connectome of the Drosophila central complex reveals network motifs suitable for flexible navigation and context-dependent action selection”, eLife 10:e66039**  
DOI: `10.7554/eLife.66039`. 용도: 부분 회로 선택의 신경과학적 근거. 논문과 FAFB의 뉴런 ID가 동일하다는 뜻으로 사용하지 않았다.  
`https://elifesciences.org/articles/66039`

**[S12] Lappalainen et al. (2024), “Connectome-constrained networks predict neural activity across the fly visual system”, Nature 634, 1132–1140**  
DOI: `10.1038/s41586-024-07939-3`. 용도: 연결 제약과 동역학·학습의 결합, graded visual neuron 처리의 중요성.  
`https://www.nature.com/articles/s41586-024-07939-3`

**[S13] Jin et al. (2026), “Whole-Brain Connectomic Graph Model Enables Whole-Body Locomotion Control in Fruit Fly” — FlyGM**  
검토 버전: arXiv `2602.17997v3`, 2026-06-14. 최초 공개: 2026-02-20. 용도: 연결망 기반 제어 구조, 모방→PPO 학습, 구조 대조군의 선례. 프리프린트이며 BlackOut 효과를 입증하는 자료가 아니다.  
`https://arxiv.org/abs/2602.17997v3`  
본문: `https://arxiv.org/html/2602.17997v3`

**[S14] Huang et al. (2024), “Dopamine-mediated interactions between short- and long-term memory dynamics”, Nature 634, 1141–1149**  
DOI: `10.1038/s41586-024-07819-w`. 용도: DOOMFLY가 인용하는 가소성·기억 연구의 출처 확인 및 F2의 후속 검토 자료. 이 계획은 해당 논문의 전체 모델이나 가소성 상수를 재현했다고 주장하지 않는다.  
서지·출처: `https://pubmed.ncbi.nlm.nih.gov/39038490/`  
저자 기관 공개 기록: `https://digitalcommons.wustl.edu/oa_4/4373/`

**[S15] Schlegel et al. (2024), “Whole-brain annotation and multi-connectome cell typing of Drosophila”, Nature**  
DOI: `10.1038/s41586-024-07686-5`. 용도: 세포 유형·데이터셋 간 대응 확인의 출발점. 구체적인 v7 부분 회로의 추출·ID 검증은 구현 단계의 작업으로 남겨뒀다.  
`https://www.nature.com/articles/s41586-024-07686-5`

---

문서 완료 범위: 공개 코드·문헌에 근거한 두 경로의 구현 및 실험 설계, 검증 조건, 예산 제안과 출처 정리.  
미실행 범위: 실제 커넥톰 파일 감사, 새 Python 모듈 작성, Unity 학습·평가, 성능 비교, 제출 검증.  
저장소 권장 배치: `reports/v7/v7_implementation_experiment_plan.md`
